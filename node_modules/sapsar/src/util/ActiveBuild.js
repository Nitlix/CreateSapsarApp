const{genChars:genChars}=require("./Random"),builds={};function createUniqueBuild(){let id=genChars(12);for(;builds[id];)id=genChars(12);return builds[id]={processes:[]},id}function removeBuild(build){delete builds[build]}function addProcess(build,process,args){builds[build].processes.push({process:process,args:args})}function getBuild(id){return builds[id]}function getBuildProcesses(build){return builds[build].processes}module.exports={createUniqueBuild:createUniqueBuild,removeBuild:removeBuild,addProcess:addProcess,getBuild:getBuild,getBuildProcesses:getBuildProcesses};