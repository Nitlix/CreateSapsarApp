const fs = require('fs');
const path = require('path');

async function exportCache(cache){
    await fs.writeFileSync(path.join(__dirname, '../../../../sapsar.json'), JSON.stringify(cache))
}

module.exports = exportCache;