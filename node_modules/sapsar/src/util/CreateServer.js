const express=require("express"),cors=require("cors"),path=require("path");function createServer(useCors="*"){const app=express();return app.disable("x-powered-by"),app.use(cors({origin:useCors})),app.use(((req,res,next)=>{res.append("x-powered-by","Sapsar"),next()})),app.use(express.static(path.join(__dirname,"../../../../public"))),app}module.exports=createServer;