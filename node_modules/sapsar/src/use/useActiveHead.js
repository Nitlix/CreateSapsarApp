import { cache } from '../src/util/SapsarCompiler.js';


export default async function (page, settings){    
    if (!cache.activeHead[page]) cache.activeHead[page] = ' ';
    cache.activeHead[page] += settings;
    return true
}